// Breakpoint values from tailwind.config.js, replacing babel-plugin-preval approach
const themeBreakpoints: Record<string, number> = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
  '3xl': 2500,
};

export default themeBreakpoints;
