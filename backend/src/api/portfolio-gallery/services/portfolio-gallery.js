'use strict';

/**
 * portfolio-gallery service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::portfolio-gallery.portfolio-gallery');
